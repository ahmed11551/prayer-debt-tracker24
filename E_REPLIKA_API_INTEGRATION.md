# 🔗 Интеграция с e-Replika API

## Настройка

### 1. Переменные окружения

Добавьте в `.env` файл:

```env
# Базовый URL e-Replika API
VITE_API_BASE_URL=https://bot.e-replika.ru/api

# API ключ (если требуется)
VITE_E_REPLIKA_API_KEY=your_api_key_here

# Токен для авторизации (опционально)
VITE_API_TOKEN=your_token_here
```

### 2. Авторизация

API поддерживает два способа авторизации:

1. **Telegram WebApp initData** (автоматически, если приложение запущено в Telegram)
2. **API ключ** через переменную окружения `VITE_E_REPLIKA_API_KEY`
3. **Bearer токен** через переменную окружения `VITE_API_TOKEN`

## Используемые эндпоинты

### 1. Получение терминов
```
GET /api/terms
```
- **Описание**: Получает словарик исламских терминов
- **Использование**: Компонент `TermsDictionary`
- **Fallback**: Если API недоступен, используются дефолтные термины

### 2. Конвертация в хиджру
```
POST /api/calendar/convert-to-hijri
Body: { date: "YYYY-MM-DD" }
Response: { year: number, month: number, day: number }
```
- **Описание**: Конвертирует григорианскую дату в хиджру
- **Использование**: Функция `calculateBulughDate` в `prayer-calculator.ts`
- **Fallback**: Упрощенная конвертация, если API недоступен

### 3. Конвертация из хиджры
```
POST /api/calendar/convert-from-hijri
Body: { year: number, month: number, day: number }
Response: { date: "YYYY-MM-DD" }
```
- **Описание**: Конвертирует дату хиджры в григорианский календарь
- **Использование**: Функция `convertToGregorian` в `prayer-calculator.ts`

### 4. Генерация PDF отчета
```
POST /api/reports/pdf
Body: { user_id: string, data?: UserPrayerDebt }
Response: Blob (PDF файл)
```
- **Описание**: Генерирует PDF отчет о долге намазов
- **Использование**: Компонент `ReportsSection`
- **Fallback**: Внутренний API эндпоинт `/api/prayer-debt/report.pdf`

## Обработка ошибок

Все функции API имеют обработку ошибок:

1. **Сетевые ошибки**: Логируются в консоль, используется fallback
2. **404 ошибки**: Для терминов возвращаются дефолтные значения
3. **Другие ошибки**: Пробрасываются для обработки в компонентах

## Тестирование

### Проверка подключения

1. Откройте консоль браузера (F12)
2. Проверьте запросы к `bot.e-replika.ru/api`
3. Убедитесь, что заголовки авторизации отправляются

### Проверка эндпоинтов

```javascript
// В консоли браузера
import { eReplikaAPI } from './src/lib/api';

// Тест получения терминов
eReplikaAPI.getTerms().then(terms => console.log('Terms:', terms));

// Тест конвертации даты
eReplikaAPI.convertToHijri(new Date()).then(hijri => console.log('Hijri:', hijri));
```

## Troubleshooting

### Проблема: API не отвечает

**Решение**: 
- Проверьте переменные окружения
- Убедитесь, что URL правильный: `https://bot.e-replika.ru/api`
- Проверьте CORS настройки на сервере

### Проблема: 401 Unauthorized

**Решение**:
- Проверьте API ключ в `.env`
- Убедитесь, что токен передается в заголовках
- Проверьте формат авторизации (Bearer token или X-API-Key)

### Проблема: 404 Not Found

**Решение**:
- Проверьте правильность эндпоинтов
- Убедитесь, что версия API актуальна
- Используйте fallback значения

### Проблема: CORS ошибки

**Решение**:
- API должен разрешать запросы с вашего домена
- В development можно использовать прокси в `vite.config.ts`
- В production убедитесь, что домен добавлен в whitelist

## Дополнительные настройки

### Прокси для development

Если нужен прокси, добавьте в `vite.config.ts`:

```typescript
export default defineConfig({
  server: {
    proxy: {
      '/api': {
        target: 'https://bot.e-replika.ru',
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, '/api'),
      },
    },
  },
});
```

## Статус интеграции

✅ **Работает**:
- Получение терминов (с fallback)
- Конвертация дат (с fallback)
- Генерация PDF отчетов (с fallback)

✅ **Обработка ошибок**: Все функции имеют fallback механизмы

✅ **Авторизация**: Поддержка Telegram WebApp и API ключей

