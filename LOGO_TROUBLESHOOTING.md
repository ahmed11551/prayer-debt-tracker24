# 🔍 Решение проблем с логотипом и названием

## Проблема: Логотип и название не обновляются

### Решение 1: Очистка кеша

1. **В браузере:**
   - Нажмите `Ctrl + Shift + R` (жесткая перезагрузка)
   - Или `Ctrl + F5`
   - Или откройте DevTools (F12) → Network → поставьте галочку "Disable cache"

2. **В Vercel:**
   - Откройте проект в Vercel Dashboard
   - Settings → General → Clear Build Cache
   - Запустите новый деплой

### Решение 2: Проверка URL логотипа

Логотип загружается из: `https://cdn1.ozone.ru/s3/multimedia-v/6091638319.jpg`

**Проверьте:**
1. Откройте URL в браузере - должно загружаться изображение
2. Если не загружается - возможно проблема с CORS или доступом

### Решение 3: Локальная загрузка логотипа

Если CDN не работает, загрузите логотип локально:

1. Скачайте изображение: `https://cdn1.ozone.ru/s3/multimedia-v/6091638319.jpg`
2. Сохраните в `public/logo.jpg`
3. Обновите код:

```tsx
<img 
  src="/logo.jpg" 
  alt="Logo" 
  className="w-full h-full object-cover"
/>
```

### Решение 4: Проверка кода

Убедитесь, что в `src/components/layout/MainHeader.tsx`:

- ✅ Название: `"Трекер намазов"` (не "MubarakWay")
- ✅ Подзаголовок: `"Prayer-Debt"` (не "Трекер пропущенных намазов")
- ✅ Логотип: URL из CDN или локальный файл

### Решение 5: Пересборка проекта

```bash
# Локально
npm run build

# В Vercel
# Запустите новый деплой через Dashboard
```

## Текущий код (должен быть)

```tsx
// src/components/layout/MainHeader.tsx
const getTitle = () => {
  if (location.pathname === "/dhikr") return "Зикры";
  return "Трекер намазов"; // ✅ Правильно
};

const getDescription = () => {
  if (location.pathname === "/dhikr") return "Дуа, азкары, салаваты и калимы";
  return "Prayer-Debt"; // ✅ Правильно
};
```

## Проверка после исправления

1. Откройте приложение
2. Проверьте хедер:
   - Логотип должен быть виден
   - Название: "Трекер намазов"
   - Подзаголовок: "Prayer-Debt"
3. Откройте консоль браузера (F12) → Console
4. Проверьте, нет ли ошибок загрузки изображения

## Если ничего не помогает

1. Проверьте последний коммит в GitHub
2. Убедитесь, что деплой в Vercel завершился успешно
3. Проверьте логи деплоя в Vercel Dashboard
4. Попробуйте загрузить логотип локально (см. Решение 3)

