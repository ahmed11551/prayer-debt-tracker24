# 🚀 Деплой на Vercel

## Быстрый деплой

### Способ 1: Через GitHub (Рекомендуется)

1. **Подключите репозиторий к Vercel:**
   - Зайдите на [vercel.com](https://vercel.com)
   - Войдите через GitHub
   - Нажмите "Add New Project"
   - Выберите репозиторий `ahmed11551/prayer-debt-tracker`
   - Vercel автоматически определит настройки

2. **Настройте переменные окружения:**
   - В настройках проекта перейдите в "Environment Variables"
   - Добавьте:
     - `VITE_API_BASE_URL` = `https://bot.e-replika.ru/api`
     - `VITE_INTERNAL_API_URL` = `/api`
     - `VITE_API_TOKEN` = `your_token_here` (если нужен)

3. **Деплой:**
   - Нажмите "Deploy"
   - Vercel автоматически соберет и задеплоит проект
   - После деплоя вы получите URL вида: `https://your-project.vercel.app`

### Способ 2: Через Vercel CLI

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите в аккаунт
vercel login

# Деплой
vercel

# Для продакшена
vercel --prod
```

## ⚙️ Настройки для Telegram Mini App

После деплоя на Vercel:

1. **Получите URL приложения** (например: `https://prayer-debt-tracker.vercel.app`)

2. **Настройте в Telegram Bot:**
   - Откройте [@BotFather](https://t.me/BotFather)
   - Используйте команду `/newapp` или `/setmenubutton`
   - Укажите URL вашего приложения на Vercel

3. **Настройте домен (опционально):**
   - В Vercel Dashboard → Settings → Domains
   - Добавьте свой домен
   - Настройте DNS записи

## 📝 Важные замечания

- ✅ Vercel автоматически определит Vite проект
- ✅ Файл `vercel.json` уже настроен для SPA роутинга
- ✅ Все статические файлы будут кешироваться
- ✅ HTTPS включен по умолчанию
- ✅ Автоматические деплои при push в main ветку

## 🔧 Переменные окружения

В Vercel Dashboard → Settings → Environment Variables добавьте:

```
VITE_API_BASE_URL=https://bot.e-replika.ru/api
VITE_INTERNAL_API_URL=/api
VITE_API_TOKEN=your_token_here (опционально)
```

## 📱 Тестирование после деплоя

1. Откройте URL приложения в браузере
2. Проверьте все функции
3. Протестируйте в Telegram Mini App
4. Проверьте работу API интеграций

## 🎉 Готово!

После деплоя ваше приложение будет доступно по URL от Vercel и готово к использованию в Telegram Mini App!

