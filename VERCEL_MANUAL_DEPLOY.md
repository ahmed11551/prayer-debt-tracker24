# 🚀 Принудительный деплой в Vercel

## Проблема: Изменения не отображаются в Vercel

Если изменения запушены в GitHub, но не отображаются в Vercel, выполните следующие шаги:

## Решение 1: Ручной деплой через Vercel Dashboard

1. **Откройте Vercel Dashboard:**
   - Зайдите на [vercel.com](https://vercel.com)
   - Войдите в аккаунт
   - Найдите ваш проект `prayer-debt-tracker`

2. **Запустите ручной деплой:**
   - Откройте вкладку **Deployments**
   - Нажмите кнопку **Redeploy** (в правом верхнем углу)
   - Или нажмите на три точки (⋯) у последнего деплоя → **Redeploy**

3. **Проверьте деплой:**
   - Дождитесь завершения сборки
   - Проверьте логи на наличие ошибок
   - Откройте URL приложения

## Решение 2: Через Vercel CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в аккаунт
vercel login

# Перейдите в директорию проекта
cd C:\Users\Dev-Ops\Desktop\prayer-debt-tracker-main

# Задеплойте в продакшен
vercel --prod
```

## Решение 3: Проверка подключения репозитория

1. **В Vercel Dashboard:**
   - Откройте **Settings → Git**
   - Убедитесь, что репозиторий подключен: `ahmed11551/prayer-debt-tracker`
   - Проверьте, что ветка: `main`

2. **Если репозиторий не подключен:**
   - Нажмите **Connect Git Repository**
   - Выберите `ahmed11551/prayer-debt-tracker`
   - Нажмите **Import**

## Решение 4: Проверка настроек сборки

В **Settings → General → Build & Development Settings** проверьте:

- **Framework Preset**: `Vite` (или `Other`)
- **Build Command**: `npm run build`
- **Output Directory**: `dist`
- **Install Command**: `npm install`
- **Root Directory**: `./` (корень проекта)

## Решение 5: Очистка кеша

1. **В Vercel Dashboard:**
   - Откройте последний деплой
   - Нажмите на три точки (⋯)
   - Выберите **Redeploy** с опцией **Use existing Build Cache** = `No`

2. **Или через CLI:**
```bash
vercel --prod --force
```

## Проверка последнего коммита

Убедитесь, что последний коммит в GitHub:
- `f500b47` - feat: Update logo and branding

Проверьте на GitHub: https://github.com/ahmed11551/prayer-debt-tracker/commits/main

## Что должно измениться после деплоя

1. **Логотип:** Вместо иконки Sparkles должен быть логотип из CDN
2. **Название:** "Трекер намазов" вместо "MubarakWay"
3. **Подзаголовок:** "Prayer-Debt" вместо "Трекер пропущенных намазов"

## Если ничего не помогает

1. **Проверьте логи деплоя:**
   - Откройте последний деплой в Vercel
   - Проверьте логи сборки на ошибки

2. **Проверьте переменные окружения:**
   - Settings → Environment Variables
   - Убедитесь, что все нужные переменные добавлены

3. **Свяжитесь с поддержкой:**
   - [Vercel Support](https://vercel.com/support)
   - Опишите проблему и приложите логи

## Быстрая проверка

После деплоя проверьте:
1. Откройте URL приложения в браузере
2. Очистите кеш браузера (Ctrl+Shift+R)
3. Проверьте, что логотип загружается
4. Проверьте название в хедере

