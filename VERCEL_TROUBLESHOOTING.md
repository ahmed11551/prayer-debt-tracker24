# 🔧 Troubleshooting Vercel Deployment

## Проверка подключения репозитория

### 1. Проверьте в Vercel Dashboard

1. Зайдите на [vercel.com](https://vercel.com)
2. Откройте ваш проект
3. Перейдите в **Settings → Git**
4. Убедитесь, что репозиторий подключен: `ahmed11551/prayer-debt-tracker`

### 2. Проверьте последний деплой

1. В Vercel Dashboard откройте вкладку **Deployments**
2. Проверьте последний коммит - должен быть `f500b47` (Update logo and branding)
3. Если деплой не запустился автоматически, нажмите **Redeploy**

## Ручной запуск деплоя

### Вариант 1: Через Vercel Dashboard

1. Откройте проект в Vercel
2. Перейдите в **Deployments**
3. Нажмите на три точки (⋯) у последнего деплоя
4. Выберите **Redeploy**
5. Или нажмите **Redeploy** в правом верхнем углу

### Вариант 2: Через Vercel CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в аккаунт
vercel login

# Перейдите в директорию проекта
cd C:\Users\Dev-Ops\Desktop\prayer-debt-tracker-main

# Задеплойте
vercel --prod
```

### Вариант 3: Через GitHub (триггер деплоя)

```bash
# Создайте пустой коммит для триггера деплоя
git commit --allow-empty -m "trigger: Force Vercel redeploy"
git push origin main
```

## Проверка настроек сборки

### Build Settings в Vercel

Убедитесь, что в **Settings → General → Build & Development Settings**:

- **Framework Preset**: Vite
- **Build Command**: `npm run build`
- **Output Directory**: `dist`
- **Install Command**: `npm install`

## Проверка переменных окружения

В **Settings → Environment Variables** должны быть (если нужны):

```
VITE_API_BASE_URL=https://bot.e-replika.ru/api
VITE_INTERNAL_API_URL=/api
VITE_E_REPLIKA_API_KEY=your_key (если нужен)
VITE_API_TOKEN=your_token (если нужен)
```

## Проверка логов деплоя

1. Откройте последний деплой в Vercel
2. Нажмите на деплой, чтобы увидеть логи
3. Проверьте:
   - Успешно ли прошла установка зависимостей
   - Успешно ли прошла сборка
   - Есть ли ошибки

## Частые проблемы

### Проблема: Деплой не запускается автоматически

**Решение:**
- Проверьте, что репозиторий подключен
- Убедитесь, что вы пушите в ветку `main`
- Проверьте настройки Git в Vercel

### Проблема: Сборка падает с ошибкой

**Решение:**
- Проверьте логи сборки
- Убедитесь, что все зависимости установлены
- Проверьте, что `package.json` корректен

### Проблема: Изменения не отображаются

**Решение:**
1. Очистите кеш браузера (Ctrl+Shift+R)
2. Проверьте, что деплой завершился успешно
3. Подождите 1-2 минуты для распространения CDN

### Проблема: Логотип не загружается

**Решение:**
- Проверьте, что URL логотипа доступен: `https://cdn1.ozone.ru/s3/multimedia-v/6091638319.jpg`
- Убедитесь, что CORS разрешен для вашего домена
- Рассмотрите возможность загрузки логотипа в `public/` папку

## Быстрая проверка

Выполните эти команды для проверки:

```bash
# Проверьте последний коммит
git log --oneline -1

# Проверьте, что все запушено
git status

# Проверьте подключение к GitHub
git remote -v

# Принудительно запушьте (если нужно)
git push origin main --force-with-lease
```

## Связь с поддержкой Vercel

Если ничего не помогает:
1. Откройте [Vercel Support](https://vercel.com/support)
2. Опишите проблему
3. Приложите логи деплоя

